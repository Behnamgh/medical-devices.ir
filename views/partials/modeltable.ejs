<div class="col-md-2 col-md-offset-1" ng-repeat="item in devicefinder.lists">
      <div class="panel panel-default">
        <div class="panel-heading text-center">{{item.model}}</div>
        <div class="panel-body text-center">{{item.ob}}</div>
      </div>
  </div>
  <div ui-view></div>
